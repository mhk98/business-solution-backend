const PackagingMixerFilterAbleFileds = [
  "searchTerm",
  "startDate",
  "endDate",
  "name",
  "manufacturerId",
  "itemId",
];

const PackagingMixerSearchableFields = ["name", "manufacturerName"];

module.exports = {
  PackagingMixerFilterAbleFileds,
  PackagingMixerSearchableFields,
};
