const PackagingItemStockFilterAbleFileds = [
  "searchTerm",
  "startDate",
  "endDate",
  "name",
  "packagingItemId",
];

const PackagingItemStockSearchableFields = ["name"];

module.exports = {
  PackagingItemStockFilterAbleFileds,
  PackagingItemStockSearchableFields,
};
